# Active product: Caqli AI Web Browser

This directory **is** the canonical git repo (`github.com/saafir-coder/caqli-ai`).

- **In scope:** `apps/web`, `apps/server`, `packages/*`, `bun run dev`
- **Parked:** `apps/desktop` → use `../caqli-desktop-parked/` for Electron-only work
- **Not here:** legacy cloud MVP → `../caqli-cloud-legacy/mvp/`
